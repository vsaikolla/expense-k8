apiversion: apps/v1
kind: deployment
metadata:
  name: mysql
  labels:
    component: mysql
    project: expense-k8
    tier: database
    env: dev
spec:
  replicas: 2
  selector:
    matchLabels:
      component: mysql
      project: expense-k8
      tier: database
      env: dev
  template:
    metadata:
      labels:
        component: mysql
        project: expense-k8
        tier: database
        env: dev
    spec:
      containers:
        - name: mysql
          image: joindevops/mysql:v1.0